<div class="mb-3 text-right">
    @if (record > 0)
    {
        if (isHome)
        {
            <a href="home/@(record - 12)" class="btn bg-gray text-white">Anteriores</a>
            <a href="home" class="btn bg-gray text-white">Portada</a>
        }
        else if (isMyPets)
        {
            <a href="mis-mascotas/@(record - 12)" class="btn bg-gray text-white">Anteriores</a>
            <a href="mis-mascotas" class="btn bg-gray text-white">Portada</a>
        }
        else if (isAdmin)
        {
            <a href="admin/@(record - 12)" class="btn bg-gray text-white">Anteriores</a>
            <a href="admin" class="btn bg-gray text-white">Portada</a>
        }
    }
    @if (record + pageSize < petsCount)
    {
        if (isHome)
        {
            <a href="home/@(record + 12)" class="btn bg-gray text-white">Siguientes</a>
        }
        else if (isMyPets)
        {
            <a href="mis-mascotas/@(record + 12)" class="btn bg-gray text-white">Siguientes</a>
        }
        else if (isAdmin)
        {
            <a href="admin/@(record + 12)" class="btn bg-gray text-white">Siguientes</a>
        }
    }
</div>

@code {
    [Parameter]
    public int record { get; set; }
    [Parameter]
    public int pageSize { get; set; }
    [Parameter]
    public int petsCount { get; set; }

    public bool isHome { get; set; }
    public bool isMyPets { get; set; }
    public bool isAdmin { get; set; }
}
